# ============================================================================
# TaskGuard - Dependências de Produção
# Versões fixadas (pinning) para builds reprodutíveis e auditáveis.
# ============================================================================

# --- Núcleo do framework web ---
Flask==3.0.3
Werkzeug==3.0.3

# --- Persistência / ORM ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.30

# --- Driver PostgreSQL (psycopg 3). O binário evita compilar libpq no build.
# A URL usa o dialeto "postgresql+psycopg://" (ver config.py / .env.example).
psycopg[binary]==3.2.1

# --- Autenticação e sessão ---
Flask-Login==0.6.3

# --- Formulários, CSRF e validação ---
Flask-WTF==1.2.1
WTForms==3.1.2
email-validator==2.1.1

# --- Segurança: headers HTTP / CSP / HSTS ---
flask-talisman==1.1.0

# --- Segurança: rate limiting / proteção brute force ---
Flask-Limiter==3.7.0
limits==3.12.0

# --- Servidor WSGI de produção ---
gunicorn==22.0.0

# --- Variáveis de ambiente ---
python-dotenv==1.0.1
