# ============================================================================
# TaskGuard - Dependências de Desenvolvimento, Teste e Segurança (SAST)
# Instaladas apenas em ambientes de CI e desenvolvimento, nunca em produção.
# ============================================================================

-r requirements.txt

# --- Testes ---
pytest==8.2.2
pytest-cov==5.0.0
pytest-flask==1.3.0

# --- Qualidade de código / lint ---
flake8==7.1.0
flake8-bugbear==24.4.26

# --- SAST (Static Application Security Testing) ---
bandit[toml]==1.7.9

# --- Relatório acadêmico ---
# O relatório é escrito em LaTeX (docs/relatorio_academico.tex) e compilado
# com uma distribuição TeX (TeX Live / MiKTeX) ou no Overleaf. Não há
# dependência Python para gerá-lo.
